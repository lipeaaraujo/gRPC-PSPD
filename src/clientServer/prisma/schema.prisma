generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://user:12345@localhost:5432/db_grpc_client"
}

model User {
    id String @id
    name String
    credit_limit Float
    balance Float
}
